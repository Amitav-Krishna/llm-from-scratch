#ifndef HASHMAP_HPP
#define HASHMAP_HPP
#include "vector.hpp"

// These earlier lines ensure that this file has not been included more than once in the same compilation

int hash_code(vectorClass<char> chars);
mathVector sieve_of_eratosthenes(int limit) ;

#endif
